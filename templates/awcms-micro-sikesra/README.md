# awcms-micro-sikesra Template Scaffold

This directory reserves the canonical non-Cloudflare template path requested by `tmp/prompt-refactor`.

The runnable SIKESRA implementation has not been moved here yet.
