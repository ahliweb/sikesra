# awcms-micro-sikesra Cloudflare Template Scaffold

This directory reserves the canonical Cloudflare template path requested by `tmp/prompt-refactor`.

The active Cloudflare runtime remains wired through `demos/cloudflare/` until the template migration is completed.
