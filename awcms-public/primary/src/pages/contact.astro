---
import { defaultLocale } from "~/utils/i18n";
export const prerender = true;
---

<meta http-equiv="refresh" content={`0;url=/${defaultLocale}/contact`} />
<script is:inline define:vars={{ defaultLocale }}>
  window.location.href = `/${defaultLocale}/contact`;
</script>
