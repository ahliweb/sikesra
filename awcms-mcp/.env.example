# Supabase Database Connection String
# Get this from Supabase Dashboard -> Project Settings -> Database -> Connection string -> Direct connection
# NOTE: Use port 5432 (Session mode) for MCP to ensure prepared statements work correctly.
SUPABASE_DB_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres

# Context7 AI Documentation Key
CONTEXT7_API_KEY=your-context7-api-key
