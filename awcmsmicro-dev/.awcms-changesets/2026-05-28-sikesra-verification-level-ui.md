---
"@awcms-micro/plugin-sikesra": patch
---

Shows SIKESRA input and approver levels directly in the verification queue and reference verification events so the staged review chain is visible in the admin UI.
