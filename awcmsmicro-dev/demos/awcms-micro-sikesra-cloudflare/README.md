# awcms-micro-sikesra Cloudflare Demo Scaffold

This directory reserves the canonical demo path for the parent-repository refactor.

The active validation target remains `demos/cloudflare/` until the demo runtime is migrated.
