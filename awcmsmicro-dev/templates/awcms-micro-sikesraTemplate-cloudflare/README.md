# awcms-micro-sikesraTemplate Cloudflare Template Scaffold

This directory reserves the canonical Cloudflare template path for the downstream AWCMS-Micro SIKESRA template.

The active Cloudflare runtime remains wired through `demos/cloudflare/` until the template migration is completed.
