# awcms-micro-sikesra (ARCHIVED — Juni 2026)

> **This directory is no longer the canonical architecture index.** It describes a planned rename to `packages/plugins/awcms-micro-sikesra/` and a `packages/plugins/sikesra/` implementation with dedicated `security/` modules — neither ever landed on disk. The pointer to `docs/sikesra/` below is also dead (that directory doesn't exist). See `docs/prd/03.PLUGIN_ARCHITECTURE.md` §8a for the full three-generation naming history.
>
> **Canonical reference is now `docs/prd/`** (repository root) — start at `docs/prd/20.MASTER_DOCUMENT_INDEX.md`.

Kept below as a historical record only.

---

This directory is the canonical AWCMS-Micro architecture index for the SIKESRA implementation.

- Architecture: `architecture.md`
- Data model: `data-model.md`
- Security: `security-rbac-abac.md`
- UAT: `uat.md`

Detailed product and implementation references remain in `docs/sikesra/`.
