# awcms-micro-sikesra E2E Scaffold (ARCHIVED — Juni 2026)

> This reserved path was never used — no dedicated SIKESRA E2E coverage was ever moved here, and the `awcms-micro-sikesra` naming it reserved was superseded by `awcms-sikesra`. See `docs/prd/03.PLUGIN_ARCHITECTURE.md` §8a for the naming history and `docs/prd/02.IMPLEMENTATION_BACKLOG.md` EPIC-H3 for current test-coverage backlog (which lives in `awcmsmicro-dev/packages/plugins/awcms-sikesra/tests/`, not here).

This directory reserves the canonical end-to-end test path requested by `tmp/prompt-refactor`.

Current SIKESRA validation continues to run through the existing workspace test harness until dedicated SIKESRA E2E coverage is moved here.
