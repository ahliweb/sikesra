<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>SIKESRA — Sistem Informasi Kesejahteraan Rakyat</title>
<meta name="description" content="Data agregat kesejahteraan rakyat yang telah diverifikasi.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
:root{--bg:#f0f4f8;--surface:#fff;--text:#0f172a;--muted:#64748b;--accent:#0284c7;--accent2:#0ea5e9;--accent-soft:rgba(2,132,199,.08);--border:#e2e8f0;--success:#059669;--warn:#d97706;--danger:#dc2626;--radius:12px;--shadow:0 1px 3px rgba(0,0,0,.06),0 1px 2px rgba(0,0,0,.04);--shadow-lg:0 10px 25px rgba(0,0,0,.08)}
@media(prefers-color-scheme:dark){:root{--bg:#0c1222;--surface:#1a2332;--text:#e2e8f0;--muted:#94a3b8;--accent:#38bdf8;--accent2:#7dd3fc;--accent-soft:rgba(56,189,248,.1);--border:#1e3a5f;--shadow:0 1px 3px rgba(0,0,0,.3);--shadow-lg:0 10px 25px rgba(0,0,0,.4)}}
*{box-sizing:border-box;margin:0;padding:0}
body{font-family:'Inter',system-ui,sans-serif;background:var(--bg);color:var(--text);line-height:1.6;-webkit-font-smoothing:antialiased}
.hero{background:linear-gradient(135deg,#0c4a6e 0%,#0284c7 50%,#0ea5e9 100%);color:#fff;padding:3rem 1.5rem 2.5rem;text-align:center;position:relative;overflow:hidden}
.hero::before{content:'';position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:radial-gradient(circle,rgba(255,255,255,.05) 0%,transparent 60%);animation:pulse 8s ease-in-out infinite}
@keyframes pulse{0%,100%{transform:scale(1)}50%{transform:scale(1.1)}}
.hero h1{font-size:clamp(1.8rem,4vw,2.5rem);font-weight:800;letter-spacing:-.02em;position:relative}
.hero .subtitle{font-size:1rem;opacity:.85;margin-top:.5rem;position:relative}
.hero .badge{display:inline-block;background:rgba(255,255,255,.15);backdrop-filter:blur(4px);padding:.25rem .75rem;border-radius:20px;font-size:.75rem;margin-top:.75rem;position:relative}
.wrap{max-width:960px;margin:0 auto;padding:1.5rem 1rem}
.notice{background:var(--accent-soft);border:1px solid var(--border);border-left:4px solid var(--accent);padding:.75rem 1rem;border-radius:var(--radius);font-size:.82rem;color:var(--muted);margin-bottom:1.5rem}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem;margin-bottom:2rem}
.kpi{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:1.25rem;text-align:center;box-shadow:var(--shadow);transition:transform .2s,box-shadow .2s}
.kpi:hover{transform:translateY(-2px);box-shadow:var(--shadow-lg)}
.kpi .val{font-size:2.2rem;font-weight:800;background:linear-gradient(135deg,var(--accent),var(--accent2));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.kpi .lbl{font-size:.78rem;color:var(--muted);margin-top:.25rem;font-weight:500;text-transform:uppercase;letter-spacing:.04em}
h2{font-size:1.1rem;font-weight:700;margin-bottom:1rem;display:flex;align-items:center;gap:.5rem}
h2::before{content:'';width:4px;height:1.1em;background:var(--accent);border-radius:2px}
.card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:1.25rem;box-shadow:var(--shadow);margin-bottom:1.5rem}
.bar-row{display:flex;align-items:center;gap:.75rem;padding:.5rem 0;border-bottom:1px solid var(--border)}
.bar-row:last-child{border-bottom:none}
.bar-name{width:180px;font-size:.85rem;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;flex-shrink:0}
.bar-track{flex:1;height:24px;background:var(--bg);border-radius:6px;overflow:hidden;position:relative}
.bar-fill{height:100%;border-radius:6px;background:linear-gradient(90deg,var(--accent),var(--accent2));transition:width .6s ease;min-width:3px}
.bar-fill.suppressed{background:var(--border)}
.bar-count{width:40px;font-size:.82rem;font-weight:600;text-align:right;flex-shrink:0}
.bar-count.suppressed{color:var(--muted);font-style:italic}
.status-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:.75rem}
.status-chip{background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:.75rem;text-align:center;font-size:.8rem}
.status-chip .sv{font-size:1.4rem;font-weight:700;color:var(--accent)}
.status-chip .sl{color:var(--muted);font-size:.72rem;text-transform:uppercase;letter-spacing:.03em;margin-top:.15rem}
.caveat{background:var(--accent-soft);border:1px solid var(--border);border-radius:var(--radius);padding:1rem;margin-top:1.5rem;font-size:.8rem;color:var(--muted)}
.caveat h3{font-size:.85rem;font-weight:600;color:var(--text);margin-bottom:.35rem}
footer{text-align:center;padding:2rem 1rem;font-size:.72rem;color:var(--muted);border-top:1px solid var(--border);margin-top:2rem}
footer a{color:var(--accent);text-decoration:none}
.loading{text-align:center;padding:2rem;color:var(--muted);font-size:.85rem}
.err{background:#fef2f2;border:1px solid #fecaca;color:#991b1b;padding:1rem;border-radius:var(--radius);text-align:center;font-size:.85rem}
@media(prefers-color-scheme:dark){.err{background:#450a0a;border-color:#7f1d1d;color:#fca5a5}}
@media(max-width:600px){.grid{grid-template-columns:1fr 1fr}.bar-name{width:120px}.hero{padding:2rem 1rem 1.5rem}}
</style>
</head>
<body>
<div class="hero">
<h1>SIKESRA</h1>
<p class="subtitle" id="meta-desc">Sistem Informasi Kesejahteraan Rakyat</p>
<div class="badge" id="meta-update"></div>
</div>
<div class="wrap" id="main-content">
<div class="notice">Halaman ini menampilkan data agregat yang telah diverifikasi. Data pribadi, alamat detail, dan informasi sensitif tidak ditampilkan.</div>
<div class="grid" id="kpi-grid">
<div class="kpi"><div class="val" id="kpi-total">—</div><div class="lbl">Total Data</div></div>
<div class="kpi"><div class="val" id="kpi-verified">—</div><div class="lbl">Terverifikasi</div></div>
<div class="kpi"><div class="val" id="kpi-villages">—</div><div class="lbl">Desa Aktif</div></div>
<div class="kpi"><div class="val" id="kpi-update">—</div><div class="lbl">Pembaruan</div></div>
</div>
<h2>Kategori Data</h2>
<div class="card" id="chart-types"><div class="loading">Memuat data…</div></div>
<h2>Status Verifikasi</h2>
<div class="card"><div class="status-grid" id="chart-verif"><div class="loading">Memuat…</div></div></div>
<div class="caveat"><h3>Catatan</h3><p id="caveat-text"></p><p id="caveat-scope" style="margin-top:.4rem"></p></div>
</div>
<footer><p id="footer-contact"></p><p>Didukung oleh <a href="/_emdash/admin">AWCMS-Micro &amp; EmDash</a></p></footer>
<script>
var A="/_emdash/api/plugins/sikesra";
function ge(id){return document.getElementById(id)}
function fmt(v){return v!=null?v.toLocaleString("id-ID"):"—"}
function ago(t){if(!t)return"—";var d=Math.floor((Date.now()-new Date(t).getTime())/864e5);return d===0?"Hari ini":d===1?"Kemarin":d+" hari lalu"}
async function load(u){var r=await fetch(u);var j=await r.json();if(!j.ok)throw new Error(j.error?.message||"Gagal");return j.data}
(async function(){
try{
var m=await load(A+"/public/metadata");
var s=await load(A+"/public/summary");
ge("meta-desc").textContent=m.description||m.title||"Sistem Informasi Kesejahteraan Rakyat";
ge("meta-update").textContent=m.latestUpdateAt?"Diperbarui "+ago(m.latestUpdateAt):(s.kpis.latestUpdateAt?"Diperbarui "+ago(s.kpis.latestUpdateAt):"");
ge("footer-contact").textContent=m.officialContact?"Kontak: "+m.officialContact:"";
ge("kpi-total").textContent=fmt(s.kpis.totalEntities);
ge("kpi-verified").textContent=fmt(s.kpis.verifiedEntities);
ge("kpi-villages").textContent=fmt(s.kpis.activeVillages);
ge("kpi-update").textContent=ago(s.kpis.latestUpdateAt);
ge("caveat-text").textContent=s.caveat||"";
ge("caveat-scope").textContent=m.dataScopeNote||"";
var c=ge("chart-types");
var types=s.charts&&s.charts.byObjectType;
if(types&&types.length){
var mx=Math.max.apply(null,types.map(function(t){return t.total||0}))||1;
c.innerHTML=types.map(function(t){
var pct=t.suppressed?10:Math.max(3,Math.round((t.total/mx)*100));
var cls=t.suppressed?" suppressed":"";
var val=t.suppressed?"<5":fmt(t.total);
return '<div class="bar-row"><span class="bar-name">'+t.name+'</span><div class="bar-track"><div class="bar-fill'+cls+'" style="width:'+pct+'%"></div></div><span class="bar-count'+cls+'">'+val+'</span></div>';
}).join("");
}else{c.innerHTML='<div class="loading">Belum ada data kategori</div>'}
var vs=ge("chart-verif");
var vd=s.charts&&s.charts.byVerificationStatus;
var statusLabels={"verified":"Terverifikasi","unverified":"Belum Verifikasi","submitted_village":"Submit Desa","submitted_subdistrict":"Submit Kecamatan","submitted_regency":"Submit Kabupaten","need_revision":"Perlu Perbaikan","rejected":"Ditolak"};
if(vd&&vd.length){
vs.innerHTML=vd.map(function(v){
var label=statusLabels[v.status]||v.status;
return '<div class="status-chip"><div class="sv">'+fmt(v.cnt)+'</div><div class="sl">'+label+'</div></div>';
}).join("");
}else{vs.innerHTML='<div class="loading">—</div>'}
}catch(e){
ge("main-content").innerHTML='<div class="err">Gagal memuat data. Silakan coba lagi nanti.</div>';
console.error(e);
}
})();
</script>
</body></html>