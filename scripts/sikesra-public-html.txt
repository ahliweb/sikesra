<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>SIKESRA — Sistem Informasi Kesejahteraan Rakyat</title>
<meta name="description" content="Data agregat kesejahteraan rakyat yang telah diverifikasi.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
:root{--bg:#f0f4f8;--surface:#fff;--surface-2:#f8fafc;--text:#0f172a;--muted:#64748b;--accent:#0284c7;--accent2:#0ea5e9;--accent-soft:rgba(2,132,199,.08);--border:#e2e8f0;--success:#059669;--warn:#d97706;--danger:#dc2626;--radius:14px;--shadow:0 1px 3px rgba(0,0,0,.06),0 1px 2px rgba(0,0,0,.04);--shadow-lg:0 10px 25px rgba(0,0,0,.08)}
@media(prefers-color-scheme:dark){:root{--bg:#0c1222;--surface:#1a2332;--surface-2:#111827;--text:#e2e8f0;--muted:#94a3b8;--accent:#38bdf8;--accent2:#7dd3fc;--accent-soft:rgba(56,189,248,.1);--border:#1e3a5f;--shadow:0 1px 3px rgba(0,0,0,.3);--shadow-lg:0 10px 25px rgba(0,0,0,.4)}}
*{box-sizing:border-box;margin:0;padding:0}
body{font-family:'Inter',system-ui,sans-serif;background:var(--bg);color:var(--text);line-height:1.6;-webkit-font-smoothing:antialiased}
.hero{background:linear-gradient(135deg,#0c4a6e 0%,#0284c7 50%,#0ea5e9 100%);color:#fff;padding:3.5rem 1.5rem 3rem;position:relative;overflow:hidden}
.hero::before{content:'';position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:radial-gradient(circle,rgba(255,255,255,.05) 0%,transparent 60%);animation:pulse 8s ease-in-out infinite}
@keyframes pulse{0%,100%{transform:scale(1)}50%{transform:scale(1.1)}}
.hero-inner{max-width:1120px;margin:0 auto;position:relative}
.eyebrow{display:inline-flex;align-items:center;gap:.45rem;background:rgba(255,255,255,.15);backdrop-filter:blur(4px);padding:.28rem .8rem;border-radius:999px;font-size:.76rem;font-weight:700;letter-spacing:.03em;text-transform:uppercase}
.hero h1{font-size:clamp(2rem,4vw,3rem);font-weight:800;letter-spacing:-.03em;margin-top:1rem;max-width:720px}
.hero .subtitle{font-size:1rem;opacity:.92;margin-top:.75rem;max-width:760px}
.hero-meta{display:flex;flex-wrap:wrap;gap:.65rem;margin-top:1rem}
.hero-badge{display:inline-flex;align-items:center;gap:.35rem;background:rgba(255,255,255,.14);padding:.36rem .8rem;border-radius:999px;font-size:.78rem}
.wrap{max-width:1120px;margin:0 auto;padding:1.5rem 1rem 2.5rem}
.notice{background:var(--accent-soft);border:1px solid var(--border);border-left:4px solid var(--accent);padding:.9rem 1rem;border-radius:var(--radius);font-size:.86rem;color:var(--muted);margin-bottom:1.5rem}
.card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:1.25rem;box-shadow:var(--shadow);margin-bottom:1.5rem}
.filter-card{display:grid;gap:1rem}
.filter-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:.9rem}
.field{display:flex;flex-direction:column;gap:.4rem}
.field label{font-size:.8rem;font-weight:600;color:var(--muted)}
.field select,.field button{width:100%;border:1px solid var(--border);border-radius:10px;background:var(--surface-2);color:var(--text);padding:.7rem .8rem;font:inherit}
.field select:disabled{opacity:.7;cursor:not-allowed}
.filter-actions{display:flex;flex-wrap:wrap;gap:.75rem}
.btn-primary,.btn-secondary{border-radius:10px;padding:.78rem 1rem;font-weight:600;cursor:pointer}
.btn-primary{background:linear-gradient(135deg,var(--accent),var(--accent2));color:#fff;border:none}
.btn-secondary{background:var(--surface-2);color:var(--text);border:1px solid var(--border)}
.meta-strip{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:.75rem;margin-top:.5rem}
.meta-item{background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:.8rem}
.meta-item .mk{font-size:.74rem;color:var(--muted);text-transform:uppercase;letter-spacing:.03em}
.meta-item .mv{font-size:.92rem;font-weight:600;margin-top:.2rem}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem;margin-bottom:2rem}
.kpi{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:1.25rem;text-align:center;box-shadow:var(--shadow);transition:transform .2s,box-shadow .2s}
.kpi:hover{transform:translateY(-2px);box-shadow:var(--shadow-lg)}
.kpi .val{font-size:2.2rem;font-weight:800;background:linear-gradient(135deg,var(--accent),var(--accent2));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.kpi .lbl{font-size:.78rem;color:var(--muted);margin-top:.25rem;font-weight:500;text-transform:uppercase;letter-spacing:.04em}
h2{font-size:1.1rem;font-weight:700;margin-bottom:1rem;display:flex;align-items:center;gap:.5rem}
h2::before{content:'';width:4px;height:1.1em;background:var(--accent);border-radius:2px}
.two-col{display:grid;grid-template-columns:1.25fr .95fr;gap:1.25rem}
.bar-row{display:flex;align-items:center;gap:.75rem;padding:.55rem 0;border-bottom:1px solid var(--border)}
.bar-row:last-child{border-bottom:none}
.bar-name{width:200px;font-size:.85rem;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;flex-shrink:0}
.bar-track{flex:1;height:26px;background:var(--bg);border-radius:6px;overflow:hidden;position:relative}
.bar-fill{height:100%;border-radius:6px;background:linear-gradient(90deg,var(--accent),var(--accent2));transition:width .6s ease;min-width:3px}
.bar-fill.suppressed{background:var(--border)}
.bar-count{width:44px;font-size:.82rem;font-weight:600;text-align:right;flex-shrink:0}
.bar-count.suppressed{color:var(--muted);font-style:italic}
.status-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:.75rem}
.status-chip{background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:.8rem;text-align:center;font-size:.8rem}
.status-chip .sv{font-size:1.45rem;font-weight:700;color:var(--accent)}
.status-chip .sl{color:var(--muted);font-size:.72rem;text-transform:uppercase;letter-spacing:.03em;margin-top:.15rem}
.region-list{display:grid;gap:.7rem}
.region-item{display:flex;justify-content:space-between;gap:1rem;padding:.8rem .9rem;background:var(--surface-2);border:1px solid var(--border);border-radius:10px}
.region-item strong{font-size:.9rem}
.region-item span{font-size:.84rem;color:var(--muted)}
.info-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:1rem}
.info-card{background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:1rem}
.info-card h3{font-size:.9rem;font-weight:700;margin-bottom:.35rem}
.info-card p{font-size:.84rem;color:var(--muted)}
.caveat{background:var(--accent-soft);border:1px solid var(--border);border-radius:var(--radius);padding:1rem;margin-top:1.5rem;font-size:.82rem;color:var(--muted)}
.caveat h3{font-size:.88rem;font-weight:700;color:var(--text);margin-bottom:.35rem}
footer{text-align:center;padding:2rem 1rem;font-size:.74rem;color:var(--muted);border-top:1px solid var(--border);margin-top:2rem}
footer a{color:var(--accent);text-decoration:none}
.loading{text-align:center;padding:2rem;color:var(--muted);font-size:.85rem}
.err{background:#fef2f2;border:1px solid #fecaca;color:#991b1b;padding:1rem;border-radius:var(--radius);text-align:center;font-size:.85rem}
@media(prefers-color-scheme:dark){.err{background:#450a0a;border-color:#7f1d1d;color:#fca5a5}}
@media(max-width:860px){.two-col{grid-template-columns:1fr}.bar-name{width:140px}}
@media(max-width:600px){.grid{grid-template-columns:1fr 1fr}.hero{padding:2.5rem 1rem 2rem}.hero-meta{flex-direction:column;align-items:flex-start}}
</style>
</head>
<body>
<div class="hero">
  <div class="hero-inner">
    <div class="eyebrow">Data Agregat Publik SIKESRA</div>
    <h1 id="hero-title">SIKESRA</h1>
    <p class="subtitle" id="meta-desc">Sistem Informasi Kesejahteraan Rakyat</p>
    <div class="hero-meta">
      <div class="hero-badge" id="meta-update">Memuat pembaruan...</div>
      <div class="hero-badge" id="meta-scope">Cakupan data publik sedang dimuat</div>
    </div>
  </div>
</div>
<div class="wrap" id="main-content">
  <div class="notice">Halaman ini menampilkan data agregat yang telah diverifikasi. Data pribadi, alamat detail, identitas anak, detail disabilitas, desil individu, dan informasi sensitif lainnya tidak ditampilkan.</div>

  <div class="card filter-card">
    <h2>Filter Ringkasan</h2>
    <div class="filter-grid">
      <div class="field"><label for="filter-district">Kecamatan</label><select id="filter-district"></select></div>
      <div class="field"><label for="filter-village">Desa/Kelurahan</label><select id="filter-village"></select></div>
      <div class="field"><label for="filter-type">Jenis Data</label><select id="filter-type"></select></div>
      <div class="field"><label for="filter-year">Tahun</label><select id="filter-year"></select></div>
      <div class="field"><label for="filter-status">Status Agregat</label><select id="filter-status"></select></div>
    </div>
    <div class="filter-actions">
      <button class="btn-primary" id="apply-filters" type="button">Terapkan Filter</button>
      <button class="btn-secondary" id="reset-filters" type="button">Reset</button>
    </div>
    <div class="meta-strip">
      <div class="meta-item"><div class="mk">Filter Aktif</div><div class="mv" id="filter-count">0</div></div>
      <div class="meta-item"><div class="mk">Status Publik</div><div class="mv" id="meta-enabled">Memuat...</div></div>
      <div class="meta-item"><div class="mk">Ambang Suppression</div><div class="mv" id="suppression-threshold">—</div></div>
      <div class="meta-item"><div class="mk">Cell Tersuppress</div><div class="mv" id="suppression-count">—</div></div>
    </div>
  </div>

  <div class="grid" id="kpi-grid">
    <div class="kpi"><div class="val" id="kpi-total">—</div><div class="lbl">Total Data</div></div>
    <div class="kpi"><div class="val" id="kpi-verified">—</div><div class="lbl">Terverifikasi</div></div>
    <div class="kpi"><div class="val" id="kpi-villages">—</div><div class="lbl">Desa Aktif</div></div>
    <div class="kpi"><div class="val" id="kpi-update">—</div><div class="lbl">Pembaruan</div></div>
  </div>

  <div class="two-col">
    <div>
      <h2>Kategori Data</h2>
      <div class="card" id="chart-types"><div class="loading">Memuat data…</div></div>
    </div>
    <div>
      <h2>Sebaran Wilayah</h2>
      <div class="card" id="chart-region"><div class="loading">Memuat data…</div></div>
    </div>
  </div>

  <div class="two-col">
    <div>
      <h2>Status Verifikasi</h2>
      <div class="card"><div class="status-grid" id="chart-verif"><div class="loading">Memuat…</div></div></div>
    </div>
    <div>
      <h2>Atribut Aman</h2>
      <div class="card" id="chart-attributes"><div class="loading">Memuat data…</div></div>
    </div>
  </div>

  <h2>Informasi Program</h2>
  <div class="info-grid">
    <div class="info-card"><h3>Deskripsi Publik</h3><p id="program-description">Memuat deskripsi program...</p></div>
    <div class="info-card"><h3>Kontak Resmi</h3><p id="program-contact">Memuat kontak resmi...</p></div>
    <div class="info-card"><h3>Cakupan Data</h3><p id="program-scope">Memuat cakupan data...</p></div>
  </div>

  <div class="caveat"><h3>Catatan Privasi</h3><p id="caveat-text"></p><p id="caveat-scope" style="margin-top:.4rem"></p></div>
</div>
<footer><p>Halaman publik SIKESRA hanya menampilkan informasi agregat yang aman dipublikasikan.</p><p><a href="/_emdash/admin">Masuk Admin</a> · <a href="/sikesra">Ringkasan SIKESRA</a></p></footer>
<script>
var A="/_emdash/api/plugins/sikesra";
var state={districtCode:"",villageCode:"",objectTypeCode:"",year:"",status:""};
var allFilters={districts:[],villages:[],objectTypes:[],years:[],statuses:[]};
function ge(id){return document.getElementById(id)}
function fmt(v){return v!=null&&v!==""?Number(v).toLocaleString("id-ID"):"—"}
function ago(t){if(!t)return"—";var d=Math.floor((Date.now()-new Date(t).getTime())/864e5);return d<=0?"Hari ini":d===1?"Kemarin":d+" hari lalu"}
function esc(v){return String(v==null?"":v).replace(/[&<>'"]/g,function(c){return{"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"}[c]})}
function filterCount(){var n=0;Object.keys(state).forEach(function(k){if(state[k])n++});return n}
function queryString(){var q=new URLSearchParams();Object.keys(state).forEach(function(k){if(state[k])q.set(k,state[k])});return q.toString()}
function syncUrl(){var q=queryString();history.replaceState(null,"",q?"/sikesra?"+q:"/sikesra")}
function readUrl(){var q=new URLSearchParams(location.search);state.districtCode=q.get("districtCode")||"";state.villageCode=q.get("villageCode")||"";state.objectTypeCode=q.get("objectTypeCode")||"";state.year=q.get("year")||"";state.status=q.get("status")||""}
async function load(u){var r=await fetch(u);var j=await r.json();if(!r.ok)throw new Error(j&&j.error&&j.error.message||"Gagal");if(j&&typeof j==="object"){if("ok" in j){if(!j.ok)throw new Error(j.error&&j.error.message||"Gagal");return "data" in j?j.data:null}if("data" in j)return j.data;return j}throw new Error("Gagal")}
function setOptions(el,items,placeholder,current,disabled){el.innerHTML="";var first=document.createElement("option");first.value="";first.textContent=placeholder;el.appendChild(first);items.forEach(function(item){var opt=document.createElement("option");opt.value=String(item.value);opt.textContent=item.label;el.appendChild(opt)});el.value=current||"";el.disabled=!!disabled}
function visibleVillages(){return allFilters.villages.filter(function(v){return !state.districtCode||v.districtCode===state.districtCode})}
function renderFilterControls(){setOptions(ge("filter-district"),allFilters.districts.map(function(d){return{value:d.code,label:d.name}}),"Semua kecamatan",state.districtCode,false);setOptions(ge("filter-village"),visibleVillages().map(function(v){return{value:v.code,label:v.name}}),state.districtCode?"Semua desa/kelurahan":"Pilih kecamatan atau tampilkan semua",state.villageCode,false);setOptions(ge("filter-type"),allFilters.objectTypes.map(function(t){return{value:t.code,label:t.name}}),"Semua jenis data",state.objectTypeCode,false);setOptions(ge("filter-year"),allFilters.years.map(function(y){return{value:y,label:String(y)}}),"Semua tahun",state.year,false);setOptions(ge("filter-status"),allFilters.statuses.map(function(s){return{value:s.code,label:s.label}}),"Semua status agregat",state.status,false);ge("filter-count").textContent=String(filterCount())}
function renderBarChart(targetId,items,emptyText){var el=ge(targetId);if(!items||!items.length){el.innerHTML='<div class="loading">'+emptyText+'</div>';return}var mx=Math.max.apply(null,items.map(function(t){return t.total||0}))||1;el.innerHTML=items.map(function(t){var pct=t.suppressed?10:Math.max(3,Math.round((t.total/mx)*100));var cls=t.suppressed?" suppressed":"";var val=t.suppressed?"<"+ge("suppression-threshold").textContent:fmt(t.total);return '<div class="bar-row"><span class="bar-name">'+esc(t.label)+'</span><div class="bar-track"><div class="bar-fill'+cls+'" style="width:'+pct+'%"></div></div><span class="bar-count'+cls+'">'+esc(val)+'</span></div>'}).join("")}
function renderRegionList(items){var el=ge("chart-region");if(!items||!items.length){el.innerHTML='<div class="loading">Belum ada data wilayah</div>';return}el.innerHTML='<div class="region-list">'+items.map(function(t){var val=t.suppressed?"<"+ge("suppression-threshold").textContent:fmt(t.total);return '<div class="region-item"><div><strong>'+esc(t.label)+'</strong><span>Agregat wilayah aman publik</span></div><strong>'+esc(val)+'</strong></div>'}).join("")+'</div>'}
function renderStatus(items){var el=ge("chart-verif");if(!items||!items.length){el.innerHTML='<div class="loading">—</div>';return}var labels={verified:"Terverifikasi",active:"Aktif"};el.innerHTML=items.map(function(v){return '<div class="status-chip"><div class="sv">'+(v.suppressed?"<"+ge("suppression-threshold").textContent:fmt(v.total))+'</div><div class="sl">'+esc(labels[v.key]||v.label||v.key)+'</div></div>'}).join("")}
function renderSummary(meta,summary){ge("hero-title").textContent=meta.title||"SIKESRA";ge("meta-desc").textContent=meta.description||meta.title||"Sistem Informasi Kesejahteraan Rakyat";ge("meta-update").textContent=(meta.latestUpdateAt||summary.kpis.latestUpdateAt)?"Diperbarui "+ago(meta.latestUpdateAt||summary.kpis.latestUpdateAt):"Belum ada pembaruan";ge("meta-scope").textContent=meta.dataScopeNote||"Cakupan data agregat publik";ge("meta-enabled").textContent=meta.enabled?"Publik aktif":"Publik dibatasi";ge("program-description").textContent=meta.description||"Ringkasan publik SIKESRA.";ge("program-contact").textContent=meta.officialContact||"Kontak resmi belum tersedia";ge("program-scope").textContent=meta.dataScopeNote||"Cakupan data publik belum dijelaskan";ge("kpi-total").textContent=fmt(summary.kpis.totalEntities);ge("kpi-verified").textContent=fmt(summary.kpis.verifiedEntities);ge("kpi-villages").textContent=fmt(summary.kpis.activeVillages);ge("kpi-update").textContent=ago(summary.kpis.latestUpdateAt);ge("suppression-threshold").textContent=String(summary.suppression.threshold||5);ge("suppression-count").textContent=fmt(summary.suppression.suppressedCells||0);ge("caveat-text").textContent=summary.caveat||"";ge("caveat-scope").textContent=meta.dataScopeNote||"";renderBarChart("chart-types",summary.charts&&summary.charts.byObjectType,"Belum ada data kategori");renderRegionList(summary.charts&&summary.charts.byRegion);renderStatus(summary.charts&&summary.charts.byVerificationStatus);renderBarChart("chart-attributes",summary.charts&&summary.charts.bySafeAttribute,"Belum ada atribut aman untuk ditampilkan")}
async function refreshSummary(){var qs=queryString();var summary=await load(A+"/public/summary"+(qs?"?"+qs:""));var meta=window.__metaData;renderSummary(meta,summary);syncUrl()}
async function init(){try{readUrl();var loaded=await Promise.all([load(A+"/public/metadata"),load(A+"/public/filters")]);window.__metaData=loaded[0];allFilters=loaded[1];renderFilterControls();await refreshSummary();ge("filter-district").addEventListener("change",function(){state.districtCode=this.value||"";if(state.villageCode&&visibleVillages().every(function(v){return v.code!==state.villageCode}))state.villageCode="";renderFilterControls()});ge("filter-village").addEventListener("change",function(){state.villageCode=this.value||""});ge("filter-type").addEventListener("change",function(){state.objectTypeCode=this.value||""});ge("filter-year").addEventListener("change",function(){state.year=this.value||""});ge("filter-status").addEventListener("change",function(){state.status=this.value||""});ge("apply-filters").addEventListener("click",function(){refreshSummary().catch(handleError)});ge("reset-filters").addEventListener("click",function(){state={districtCode:"",villageCode:"",objectTypeCode:"",year:"",status:""};renderFilterControls();refreshSummary().catch(handleError)})}catch(e){handleError(e)}}
function handleError(e){ge("main-content").innerHTML='<div class="err">Gagal memuat data. Silakan coba lagi nanti.</div>';console.error(e)}
init();
</script>
</body></html>
