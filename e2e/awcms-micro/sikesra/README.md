# awcms-micro-sikesra E2E Scaffold

This directory reserves the canonical end-to-end test path requested by `tmp/prompt-refactor`.

Current SIKESRA validation continues to run through the existing workspace test harness until dedicated SIKESRA E2E coverage is moved here.
