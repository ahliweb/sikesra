# AWCMS-Micro Root Versioning

Root versioning covers repository-maintenance changes only.

Examples:

- synchronization workflow updates
- root script changes
- root governance documentation changes

Do not mix downstream plugin or template release notes into root maintenance records.
