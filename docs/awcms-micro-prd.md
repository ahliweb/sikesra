# AWCMS-Micro PRD

## Product Intent

This parent repository carries an example AWCMS-Micro downstream implementation for SIKESRA while remaining structurally aligned with EmDash and the upstream `awcms-micro` maintenance model.

## Scope

- maintain a synchronized downstream workspace
- preserve approved AWCMS-Micro and SIKESRA boundaries
- provide example plugin and template surfaces

## Out of Scope

- replacing EmDash core with a downstream fork
- introducing product behavior in root maintenance files
