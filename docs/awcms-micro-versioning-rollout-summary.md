# AWCMS-Micro Versioning Rollout Summary

The repository uses a split versioning model so parent-workspace maintenance does not obscure downstream package changes.

## Summary

- root maintenance stays root-scoped
- plugins stay plugin-scoped
- templates stay template-scoped
