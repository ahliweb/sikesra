# AWCMS-Micro Versioning

This repository separates root maintenance changes from downstream package versioning.

## Surfaces

- root repository maintenance
- plugin packages in `awcmsmicro-dev/packages/plugins/`
- template packages in `awcmsmicro-dev/templates/`

Each surface should be versioned and documented independently when applicable.
