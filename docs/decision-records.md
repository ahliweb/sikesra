# Decision Records

## Active Decisions

### Parent Workspace

This repository is maintained as a parent workspace with sibling `emdash-latest/` and `awcmsmicro-dev/` trees.

### Downstream Naming

The default downstream template name is `awcms-micro-sikesraTemplate`.

### Boundary Model

SIKESRA behavior belongs in plugin, template, demo, docs, and test boundaries inside `awcmsmicro-dev/`, not in upstream EmDash core.
