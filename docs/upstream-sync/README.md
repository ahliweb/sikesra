# Upstream Sync

This directory stores root-level notes about classifying and managing upstream synchronization work.

Use it to separate:

- pure upstream alignment work
- downstream-only implementation work
- candidate upstream contribution plans
