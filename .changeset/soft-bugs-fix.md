---
"@ahliweb/plugin-sikesra": patch
---

Fixes SIKESRA reads so admin and public data can still resolve legacy `default/default` records while deployments normalize to the canonical default tenant and site IDs.
